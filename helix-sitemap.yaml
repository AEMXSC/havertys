sitemaps:
  default:
    source: /query-index.json
    destination: /sitemap.xml
    origin: https://main--havertys--aemxsc.aem.live
    lastmod: YYYY-MM-DD
    hreflang:
      - lang: en
        prefix: /
